
const ClosedCalendar = ({ onOpen }) => {
    return (
        <div className='closed-calendar'
             onClick={onOpen}>
            <h1>Calendar</h1>
        </div>
    )
}

export default ClosedCalendar